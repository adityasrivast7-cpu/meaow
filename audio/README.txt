Put your music files here in the song folder:

- song/song 1.mp3
- song/song 2.mp3

The page loads them as browser-safe URLs, so use the actual file names and access the page via localhost instead of opening the HTML file directly.
